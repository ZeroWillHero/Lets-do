import 'package:flutter/material.dart';

class AuthCardComponent extends StatelessWidget {
  const AuthCardComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}